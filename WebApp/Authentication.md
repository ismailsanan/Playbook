

  
### Lab: Username enumeration via response timing

- username enum
- notice we get blocked after X attempts
- use `X-forwarded-for` for a random IP
- set the payload in the intruder for the `password` , `X-forwarded-for`

